- sudo:
    - dpkg --add-architecture i386
    - add-apt-repository ppa:wine/wine-builds
    - apt:
        - update
        - install:
            - --install-recommends winehq-devel
            - winetricks
